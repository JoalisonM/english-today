import { styled } from "..";

export const Container = styled("div", {
  maxWidth: "74.625rem",
  height: "calc(100vh - 6.25rem)",
  margin: "0 auto",
  padding: "2rem 2rem 0",

  display: "flex",
  flexDirection: "column",
});